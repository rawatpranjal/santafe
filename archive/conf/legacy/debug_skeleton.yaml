# @package _global_
experiment:
  name: "debug_skeleton"
  num_rounds: 1
  num_periods: 1

agents:
  buyer_types: ["Skeleton", "Skeleton"]
  seller_types: ["Skeleton", "Skeleton"]

hydra:
  verbose: true
