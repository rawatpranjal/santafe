# @package _global_
#
# Phase B: Experiment 2.2.2 - GPT-4 vs GPT-3.5 Model Comparison
# Tests intelligence premium between models

experiment:
  name: "model_comparison_gpt4_vs_gpt35"
  num_rounds: 10
  log_level: "INFO"
  rng_seed_auction: 42
  rng_seed_values: 123

market:
  num_periods: 5
  num_steps: 100
  num_tokens: 4
  min_price: 0
  max_price: 1000

agents:
  buyer_types:
    - GPT4-mini  # 2 GPT-4-mini buyers
    - GPT4-mini
    - GPT3.5     # 2 GPT-3.5 buyers
    - GPT3.5
  seller_types:
    - GPT4-mini  # 2 GPT-4-mini sellers
    - GPT4-mini
    - GPT3.5     # 2 GPT-3.5 sellers
    - GPT3.5
