# @package _global_
#
# Phase B: Experiment 2.2.1 - GPT-4 vs Legacy Traders
# Tests zero-shot GPT-4 performance against rule-based strategies

experiment:
  name: "gpt4_vs_legacy"
  num_rounds: 1  # Reduced to 1 for cost control
  log_level: "INFO"
  rng_seed_auction: 42
  rng_seed_values: 123

market:
  num_periods: 1  # Reduced to 1 for cost control
  num_steps: 20   # Reduced to 20 for cost control
  num_tokens: 4
  min_price: 0
  max_price: 1000

agents:
  buyer_types:
    - GPT4-mini  # 1 GPT-4-mini buyer (cheaper than GPT-4)
    - ZIC        # Mixed opponents
    - Kaplan
    - ZIP
  seller_types:
    - GPT4-mini  # 1 GPT-4-mini seller
    - ZIC        # Mixed opponents
    - Kaplan
    - GD
