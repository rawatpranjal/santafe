# @package _global_
#
# Phase 5: Experiment 2.2.3 - LLM Invasibility Test
# Tests profit extraction: 1 GPT-4o-mini vs 7 ZIC agents (matching legacy methodology)

experiment:
  name: "invasibility_gpt4_mini"
  num_rounds: 50  # Match legacy invasibility test sample size
  log_level: "INFO"
  rng_seed_auction: 42
  rng_seed_values: 123

market:
  num_periods: 10  # Standard period count
  num_steps: 100
  num_tokens: 4
  min_price: 0
  max_price: 1000

agents:
  buyer_types:
    - GPT4-mini  # 1 GPT-4o-mini buyer (invasive agent)
    - ZIC        # 3 ZIC buyers (naive population)
    - ZIC
    - ZIC
  seller_types:
    - GPT4-mini  # 1 GPT-4o-mini seller (invasive agent)
    - ZIC        # 3 ZIC sellers (naive population)
    - ZIC
    - ZIC
