# @package _global_
# TOK Environment: Single token per trader
# Minimal market functionality test

experiment:
  name: "tournament_tok"
  description: "Single token per trader - minimal market"
  num_rounds: 100

market:
  min_price: 1
  max_price: 1000
  num_tokens: 1     # ONLY 1 TOKEN per trader
  num_periods: 10
  num_steps: 100
  gametype: 6460  # TOK environment gametype

agents:
  num_buyers: 4
  num_sellers: 4
  buyer_types: ["Kaplan", "ZIC", "ZIP", "GD", "ZI2", "Lin", "Perry", "Jacobson"]
  seller_types: ["Kaplan", "ZIC", "ZIP", "GD", "ZI2", "Lin", "Perry", "Jacobson"]

metrics:
  track_efficiency: true
  track_price_dynamics: true
  track_volume_distribution: true
  track_profit_shares: true
  calculate_autocorrelation: true